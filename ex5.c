#include <stdio.h> // '.h' is for header files

/* This is a comment. */
int main (int argc, char *argv[]) //int for the argument count, array of char strings for arguments
{
  int distance = 100;

  //this is also a comment
  printf("You are %d miles away.\n", distance);

  return 0; //Gives the OS an exit value.
}
